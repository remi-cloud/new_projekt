services:
  www:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - CYCLICAL_SCAN_INTERVAL_MINUTES=15
    volumes:
      - trader-data:/app/data
    restart: unless-stopped

volumes:
  trader-data:
